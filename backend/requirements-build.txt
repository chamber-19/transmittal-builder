# Build-only dependencies — not required at runtime.
# Install alongside requirements.txt when building the PyInstaller sidecar:
#
#   pip install -r requirements.txt -r requirements-build.txt
#
# PyInstaller ≥ 6.10 is required for Python 3.13 support.
pyinstaller>=6.10
