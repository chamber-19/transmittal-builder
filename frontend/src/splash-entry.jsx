/**
 * splash-entry.jsx — Thin entry point for the splash screen.
 *
 * Imports the framework splash component from kc-framework@v1.0.0, which
 * self-mounts to #root.  Tool-specific config (appName, appOrg) is injected
 * via window.__SPLASH_CONFIG__ in splash.html before this module loads.
 *
 * See: @koraji95-coder/kc-framework — js/packages/kc-framework/src/splash/
 */
import "@koraji95-coder/kc-framework/splash";
