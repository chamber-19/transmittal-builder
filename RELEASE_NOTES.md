## What's new in v6.2.5

### Fixed

- Document index rows and the merged combined PDF now appear in natural
  drawing-number order, regardless of the order the OS hands files to
  the app when you drag-and-drop.

### Removed

- Internal cleanup: removed an unused legacy frontend file
  (`frontend/transmittal-builder.jsx`) that was no longer wired into
  the build. No user-visible change.
